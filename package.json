{
  "name": "analytics-dashboard",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "db:migrate": "pnpm --filter @analytics/api db:migrate",
    "db:seed": "pnpm --filter @analytics/api db:seed"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "onlyBuiltDependencies": ["esbuild"]
}
